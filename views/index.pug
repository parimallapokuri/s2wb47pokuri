extends layout

block content
  h1= title
  p Hello I'm Parimalla, My favourite hobby is to know about ancient history
  button(type="button", class="btn btn-primary",onclick="moreClaps()") Press  me
  div(id = "target") Clap for me
  a(href='/market') Market Page
  script. 
    var clap_count = 0;
    function moreClaps() {
      clap_count=clap_count+2;
      document.getElementById("target").innerHTML=`Value now ${clap_count}`;
    }